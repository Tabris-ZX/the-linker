[project]
name = "the-puzzles-server"
version = "0.2.4"
description = "FastAPI backend for THER puzzles."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "pyyaml>=6.0,<7.0"
]

[tool.uv]
package = false
