[project]
name = "the-linker-server"
version = "0.1.0"
description = "FastAPI backend for The Linker."
requires-python = ">=3.12"
dependencies = [
  "fastapi>=0.115,<1.0",
  "uvicorn[standard]>=0.30,<1.0",
  "pyyaml>=6.0,<7.0"
]

[tool.uv]
package = false
