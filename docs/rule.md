# 游戏目标

## 将所有同色的色点相连即为通关

# 操作方式

- 从一个颜色端点出发,**拖动**即可绘制路径,反向拖回可撤回线段
- 双击色点可以清除与该点相连的整条路径,双击连线中的某节点,线段会退回到此处

# 功能解释
- **辅助模式:** 开启后会对正确连接的线路进行标记
- **关联闪烁:** 开启后按住某个点,同色的另一个点会闪烁,便于找到位置

# 关卡编辑(移动端不支持)